import {
  StyleSheet
} from 'react-native';
import Dimen from '../constant/dimensions';

export let choiceLeftViewItemStyle = StyleSheet.create({
     container: {
       height: 60,
       width: 100,
       justifyContent: 'center',
       alignItems: 'center',
     },
     itemBg: {
       height: 60,
       width: 60,
     },
     itemIcon: {
       height: 60,
       width: 60,
     }
})
